userName: ${BROWSERSTACK_USERNAME}
accessKey: ${BROWSERSTACK_ACCESS_KEY}

app: ${BROWSERSTACK_APP_URL}

framework: pytest

browserstackLocal: false

projectName: Kyowon Literacy
buildName: Literacy-CI
buildIdentifier: ${GITHUB_RUN_NUMBER}
